/*import { FormArray, FormControl, FormGroup } from "@angular/forms";

export function rekordForm(): FormGroup {
    return new FormGroup({
        identifier: new FormArray([
            new FormGroup({
                use: new FormControl(),
                type: new FormGroup({
                    coding: new Form FormArray([

                    ])
                }),
            })
        ]),
    });
}*/